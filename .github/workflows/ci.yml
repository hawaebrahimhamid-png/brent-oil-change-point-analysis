name: Python CI

on:
  push:
    branches:
      - main
      - task-1*
  pull_request:
    branches:
      - main

jobs:
  test:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Verify installation
      run: |
        python -c "import pandas, numpy, matplotlib, seaborn, scipy, statsmodels, pymc, arviz; print('All packages imported successfully')"
